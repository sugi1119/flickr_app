<% provide(:title, 'Home') %>
<div id="header">
  <i class="fa fa-camera-retro fa-5x"></i>
  <h1>Flickr Photo Search!</h1>

  <div class="form">
    <%= render :partial => 'form' %>
  </div>

<div class="notice">
  <%= flash[:notice] %>
</div>

<!-- click thumbnail image and open new tab to see larger image -->
<div class="search_result">
  <% @flickr_images.each do |image| %>
  <div class="single_image">
    <!-- click thumbnail image and open new tab to see larger image -->
    <%#= link_to (image_tag(image.thumbnail_url, :size => '75x75')),image.original_url, :target => "_blank"%>

    <!-- Try to send the data to contoroller -->
    <%= link_to (image_tag(image.thumbnail_url, :size => '75x75')), limage_path(:data => image.original_url), :method => :post%>
    <%#=link_to (image_tag(image.thumbnail_url, :size => '75x75')), limage_path, :id=> "largeImage", remote: true, "method" => "post", "data-popup" => true %>
  <%#=link_to (image_tag(image.thumbnail_url, :size => '75x75')), image.original_url, limage_path, :remote => true, :method => :post, "data-popup" => true %>
  <%#= image_tag(image.thumbnail_url, :size => '75x75') %>
  <!-- <div class="pop"><%#image.original_url%></div> -->
  </div>
  <%end if @flickr_images%>
  <%#raise 'eeerere'%>
  <%#= will_paginate @page_results %>
<div>




